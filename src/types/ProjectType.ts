export type ProjectType = {
  _id: string;
  backend_url?: string;
  categories: string[];
  description: string;
  deployment_url?: string;
  download_url?: string;
  featured: boolean;
  frontend_url?: string;
  image_url: string;
  monorepo_url?: string;
  title: string;
};
